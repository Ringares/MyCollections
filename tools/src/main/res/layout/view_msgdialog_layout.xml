<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <LinearLayout
        android:id="@+id/ll_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/shape_bg_dialog_progress"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="36dp"
        android:visibility="visible">

        <com.ring.tools.custom.circularprogressbar.CircularProgressBar
            android:id="@+id/progressbar_circular"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginBottom="8dp"
            android:indeterminate="true"
            android:indeterminateOnly="false"
            app:cpb_color="@color/theme" />


        <TextView
            android:id="@+id/tv_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="正在加载"
            android:textColor="@color/black_50p" />
    </LinearLayout>

</FrameLayout>